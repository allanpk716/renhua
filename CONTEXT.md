# renhua（人话）

把看不懂的 AI 输出变成大白话的 Claude Code 技能：黑话换人话、术语就地解释、结论先行，数字/命令/代码/因果零改动。

## Language

### 触发与生效

**模型自判触发**：
模型读技能 description 后自行决定是否调用。概率性行为——长会话会忘、弱模型更差。
_Avoid_: 自动触发（一词三义，禁用）

**常驻**：
规则写进 CLAUDE.md，每个会话开场即生效，不依赖任何调用；确定性生效、说删就停。
_Avoid_: 自动触发、常开

**会话模式**：
用户喊一次（如 `/renhua on`），本次对话后续输出持续遵守规则，直到会话结束。

**三档自动化**：
让规则长期生效的三条路线：第一档 CLAUDE.md 常驻、第二档 output style、第三档 Stop 钩子。档位越高越强硬，误伤风险越大。

### 技能本体

**模式一（改写）**：
对给定文本做一次改写：列事实清单 → 按 rules.md 改 → 回读核对 → 输出改写稿+改动清单。

**模式二（提醒）**：
开启会话模式的那次调用，只回一句确认，不展开规则。

**锁事实**：
改写中不许动的部分——数字、命令、代码、因果、限定词（可能/部分/计划等不升不降）。

**共存裁决**：
与其他输出风格技能（如 i-have-adhd）同开时的优先级判定，总原则"结构让 adhd，语言让 renhua"：首行先给答案、答案可执行时答案即动作、纯问答不硬造动作；术语解释保留压一句括号、列表超 5 分组不丢内容；结尾行动项有真实下一步才给、纯问答豁免。2026-09-24 共存实验定案，三处同源（全局常驻块/README 片段/SKILL.md 边界情况）。
